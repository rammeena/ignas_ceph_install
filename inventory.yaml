all:
  children:
    osds:
      hosts:
        ceph1:
        ceph2:
        ceph3:
      # vars:
      #   ansible_user: centos
    mons:
      hosts:
        ceph1:
        ceph2:
        ceph3:
      # vars:
      #   ansible_user: centos
    mgrs:
      hosts:
        ceph1:
        ceph2:
        ceph3:
      # vars:
      #   ansible_user: centos
    # grafana-server:
    #   hosts:
    #     ceph2-grafana-pod-vmm2-fl:
    #   vars:
    #     ansible_user: centos
