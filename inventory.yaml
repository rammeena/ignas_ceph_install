all:
  children:
    osds:
      hosts:
        ceph1:
        ceph2:
        ceph3:
      # vars:
      #   ansible_user: centos
    mons:
      hosts:
        ceph1:
        ceph2:
        ceph3:
      # vars:
      #   ansible_user: centos
    mgrs:
      hosts:
        ceph1:
        ceph2:
        ceph3:
      # vars:
      #   ansible_user: centos
    rgws:
      hosts:
        ceph1:
        ceph2:
        ceph3:
      # vars:
      #   ansible_user: centos
    grafana-server:
      hosts:
        ceph1:
      # vars:
      #   ansible_user: centos
